import { ErrorDetails } from './types';
export declare function error(errorResponse: {
    [key: string]: any;
    config?: Record<string, any>;
    response?: Record<string, any>;
}): Error & ErrorDetails;
