'use strict';

var core = require('@contentstack/core');
var entries = require('../entries/entries.cjs');
var entry = require('../entries/entry.cjs');

var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
class ContentType {
  constructor(client, contentTypeUid) {
    __publicField(this, "_client");
    __publicField(this, "_contentTypeUid");
    __publicField(this, "_urlPath");
    __publicField(this, "_queryParams", {});
    this._client = client;
    this._contentTypeUid = contentTypeUid;
    this._urlPath = `/content_types/${this._contentTypeUid}`;
  }
  entry(uid) {
    if (uid) return new entry.Entry(this._client, this._contentTypeUid, uid);
    return new entries.Entries(this._client, this._contentTypeUid);
  }
  /**
   * @method fetch
   * @memberof ContentType
   * @description Fetches the contentType data on the basis of the contentType uid
   * @returns {Promise<T>} Promise that resolves to the content type data
   * @example
   * import contentstack from '@contentstack/delivery-sdk'
   *
   * const stack = contentstack.stack({ apiKey: "apiKey", deliveryToken: "deliveryToken", environment: "environment" });
   * const result = await stack.contentType(contenttype_uid).fetch();
   */
  async fetch() {
    const response = await core.getData(this._client, this._urlPath);
    if (response.content_type) return response.content_type;
    return response;
  }
}

exports.ContentType = ContentType;
//# sourceMappingURL=index.cjs.map
