import { getContentstackEndpoint } from '@contentstack/utils';

function getHostforRegion(region, host) {
  if (host) return host;
  return getContentstackEndpoint(region, "contentDelivery", true);
}
function isBrowser() {
  return typeof window !== "undefined";
}
function encodeQueryParams(params2) {
  const encodedParams = {};
  for (const [key, value] of Object.entries(params2)) {
    if (typeof value === "string") {
      encodedParams[key] = encodeURIComponent(value);
    } else if (typeof value === "object" && value !== null) {
      encodedParams[key] = encodeQueryParams(value);
    } else {
      encodedParams[key] = value;
    }
  }
  return encodedParams;
}

export { encodeQueryParams, getHostforRegion, isBrowser };
//# sourceMappingURL=utils.js.map
